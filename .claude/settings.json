{
  "permissions": {
    "deny": [
      "Bash(gh pr merge *)",
      "Bash(gh pr merge)",
      "Bash(git push origin main)",
      "Bash(git push -f *)",
      "Bash(git push --force *)"
    ]
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "cmd=$(jq -r '.tool_input.command // \"\"'); deny() { printf '{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"%s\"}}' \"$1\"; exit 0; }; printf '%s' \"$cmd\" | grep -Eq 'gh pr merge|pulls/[0-9]+/merge|gh pr review[^|;&]*--approve|gh api[^|;&]*(-X|--method|--input|-f |-F )[^|;&]*protection|gh api[^|;&]*protection[^|;&]*(-X|--method|--input|-f |-F )|gh ruleset|gh repo delete|gh repo edit' && deny \"The owner reviews and merges PRs on GitHub. Never merge, approve, or change branch protection from this session.\"; printf '%s' \"$cmd\" | grep -Eq 'git push[^|;&]*( main( |$)| HEAD:main( |$)|:main( |$)|--force|-f( |$)| \\+)' && deny \"Never push to main or force push. Open a PR from a branch instead.\"; exit 0",
            "timeout": 5,
            "statusMessage": "Checking merge guard"
          }
        ]
      }
    ]
  }
}
